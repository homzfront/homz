export const metadata = {
  title: "Switch Profile | Homz.ng",
  robots: {
    index: false,
    follow: false,
  },
};

export default function SwitchProfileLayout({ children }) {
  return children;
}