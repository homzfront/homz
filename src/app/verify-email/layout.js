export const metadata = {
  title: "Verify Your Email | Homz.ng",
  robots: {
    index: false,
    follow: false,
  },
};

export default function VerifyEmailLayout({ children }) {
  return children;
}