export const metadata = {
  title: "Reset Your Password | Homz.ng",
  robots: {
    index: false,
    follow: false,
  },
};

export default function ForgetPasswordLayout({ children }) {
  return children;
}