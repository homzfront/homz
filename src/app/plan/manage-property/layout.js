export const metadata = {
  title: "Manage Property Plan | Homz.ng",
  robots: {
    index: false,
    follow: false,
  },
};

export default function ManagePropertyPlanLayout({ children }) {
  return children;
}