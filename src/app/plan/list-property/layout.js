export const metadata = {
  title: "List Property Plan | Homz.ng",
  robots: {
    index: false,
    follow: false,
  },
};

export default function ListPropertyPlanLayout({ children }) {
  return children;
}