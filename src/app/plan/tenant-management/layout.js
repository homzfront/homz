export const metadata = {
  title: "Tenant Management Plan | Homz.ng",
  robots: {
    index: false,
    follow: false,
  },
};

export default function TenantManagementPlanLayout({ children }) {
  return children;
}