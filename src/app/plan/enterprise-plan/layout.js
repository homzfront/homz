export const metadata = {
  title: "Enterprise Plan | Homz.ng",
  robots: {
    index: false,
    follow: false,
  },
};

export default function EnterprisePlanLayout({ children }) {
  return children;
}