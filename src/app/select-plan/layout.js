export const metadata = {
  title: "Select Your Plan | Homz.ng",
  robots: {
    index: false,
    follow: false,
  },
};

export default function SelectPlanLayout({ children }) {
  return children;
}